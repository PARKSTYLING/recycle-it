@@ .. @@
         final_total_dkk: flooredScore,
         items_caught,
         correct_catches,
         wrong_catches,
-        duration_ms: 20000
+        duration_ms: 40000
       })
       .eq('id', play_session_id)